using JUPITERPA_BALANCE_TRANSACTION as JUPITERPA_BALANCE_TRANSACTION;
view JUPITERPA_BALANCE_BALANCE as select from JUPITERPA_BALANCE_TRANSACTION as TRANSACTION {
  key ACCOUNT.ID as ID,
  ACCOUNT.NAME,
  sum(AMOUNT) as AMOUNT : Integer
} group by ACCOUNT.ID, ACCOUNT.NAME;
